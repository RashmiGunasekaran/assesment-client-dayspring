<h1>All Wishlist</h1>
<% @wishlists.each do |wishlist| %>
  <div>
    <%= wishlist.product.name %>
    <%= button_to "Remove",
      wishlist_path(wishlist),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
  </div>
<% end %>

